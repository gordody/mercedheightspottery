<script lang="ts">
  // This layout component can be used to wrap all pages in the app, providing a common structure and styling.
  // You can add a header, footer, or any other elements that should be present on every page here.
export const prerender = true; // Enable prerendering for better performance and SEO
</script>

<slot />